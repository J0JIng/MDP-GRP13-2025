<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- checked half: solid white pill -->
    <item android:state_checked="true" android:color="@color/seg_white"/>
    <!-- unchecked half: transparent so the teal track shows through -->
    <item android:color="@android:color/transparent"/>
</selector>
